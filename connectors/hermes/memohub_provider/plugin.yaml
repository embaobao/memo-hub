name: memohub
display_name: MemoHub
kind: memory_provider
version: 1.0.0
entrypoint: memohub_provider.provider:MemoHubMemoryProvider
description: |
  MemoHub official Hermes memory provider. It uses the MemoHub CLI as the
  shared Connector -> Channel -> Memory entry and does not create a second
  Hermes-only data source.
hooks:
  - queue_prefetch
  - prefetch
  - sync_turn
  - on_pre_compress
  - on_session_end
  - on_memory_write
  - get_tool_schemas
  - handle_tool_call
